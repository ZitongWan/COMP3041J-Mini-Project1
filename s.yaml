# ==============================================================
# Campus Buzz - Alibaba Cloud Function Compute FC 3.0 config
# ==============================================================

edition: 3.0.0
name: campus-buzz
access: default

vars:
  region: cn-beijing
  service:
    name: campus-buzz
    description: Campus Buzz - Campus Event Submission System
    logConfig: auto

resources:
  # ----------------------------------------------------------
  # Submission Event Function
  # ----------------------------------------------------------
  submission-event-function:
    component: fc3
    props:
      region: ${vars.region}
      functionName: submission-event-function
      description: Converts new submissions into processing requests
      runtime: python3.10
      code: ./serverless/submission-event-function
      handler: app.handler
      timeout: 30
      memorySize: 256
      cpu: 0.25
      diskSize: 512
      installDependency: true
      environmentVariables:
        DATA_SERVICE_URL: "https://goldmine-washable-swimsuit.ngrok-free.dev"
        PROCESSING_FN_URL: "https://processfunction-socuuqhcyq.cn-beijing.fcapp.run"
      triggers:
        - triggerName: http-trigger
          triggerType: http
          description: HTTP trigger
          qualifier: LATEST
          triggerConfig:
            methods:
              - POST
              - GET
            authType: anonymous

  # ----------------------------------------------------------
  # Processing Function
  # ----------------------------------------------------------
  processing-function:
    component: fc3
    props:
      region: ${vars.region}
      functionName: processing-function
      description: Applies validation rules and calculates the result
      runtime: python3.10
      code: ./serverless/processing-function
      handler: app.handler
      timeout: 30
      memorySize: 256
      cpu: 0.25
      diskSize: 512
      installDependency: true
      environmentVariables:
        RESULT_UPDATE_FN_URL: "https://result-function-himtzogrsl.cn-beijing.fcapp.run"
      triggers:
        - triggerName: http-trigger
          triggerType: http
          description: HTTP trigger
          qualifier: LATEST
          triggerConfig:
            methods:
              - POST
              - GET
            authType: anonymous

  # ----------------------------------------------------------
  # Result Update Function
  # ----------------------------------------------------------
  result-update-function:
    component: fc3
    props:
      region: ${vars.region}
      functionName: result-update-function
      description: Writes processed results back to stored records
      runtime: python3.10
      code: ./serverless/result-update-function
      handler: app.handler
      timeout: 30
      memorySize: 256
      cpu: 0.25
      diskSize: 512
      installDependency: true
      environmentVariables:
        DATA_SERVICE_URL: "https://goldmine-washable-swimsuit.ngrok-free.dev"
      triggers:
        - triggerName: http-trigger
          triggerType: http
          description: HTTP trigger
          qualifier: LATEST
          triggerConfig:
            methods:
              - POST
              - GET
            authType: anonymous
