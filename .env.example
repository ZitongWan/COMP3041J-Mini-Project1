# ============================================================
# Campus Buzz - 环境变量配置示例
# ============================================================
# 复制此文件为 .env，然后填入你自己的值
#
# 部署步骤：
#   1. FC 函数部署后，s.yaml 中的 URL 变量可通过此文件配置
#   2. docker-compose 读取 .env 中的 SUBMISSION_EVENT_FN_URL
#   3. Data Service 需要通过 ngrok/localtunnel 暴露公网 URL
# ============================================================

# FC Submission Event Function 的 HTTP 触发器地址（部署 FC 后从 s.yaml 输出获取）
SUBMISSION_EVENT_FN_URL=http://host.docker.internal:9001

# Data Service 的公网地址（通过 ngrok/localtunnel 暴露本地 5002 端口）
# 示例：https://xxxx.ngrok-free.dev
# DATA_SERVICE_URL=https://your-tunnel-url.ngrok-free.dev
